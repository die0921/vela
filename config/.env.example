# No API key required.
# Chat uses Anthropic SDK (reads ANTHROPIC_API_KEY from Claude Code environment automatically).
# Embeddings use local sentence-transformers model (all-MiniLM-L6-v2), downloaded on first run.
